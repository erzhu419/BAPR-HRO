# Large files
*.pdf
# but DO track paper figures (small, source-controlled assets)
!paper/fig_*.pdf
!paper/fig_*.png
__pycache__/
*.pyc
*.egg-info/

# Cloned reference repos (too large to track)
hybrid-cp-rl-solver/
mlopt/
public-transport-statistics/
learntocut/

# Data
data/

# LaTeX build artifacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz

# Large GTFS archives — kept locally, not tracked
gtfs_fp*.zip
